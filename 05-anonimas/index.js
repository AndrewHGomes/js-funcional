function soma(a, b) {
  return `O resultado é ${a + b}`;
}

console.log(soma(50, 26));

console.log("");

//---------------------------------------

const subtrair = function (a, b) {
  return `O resultado é ${a - b}`;
};

console.log(subtrair(50, 26));

console.log("");

//---------------------------------------
